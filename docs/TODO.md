# EPIC TODO

This document tracks immediate actionable tasks that do not fit into the primary ROADMAP milestone level.

## High Priority
- [ ] Implement true timing metrics dynamically generated from `parser-v2` binary.
- [ ] Connect `--sarif` logic output stream into an official GitHub Actions component.

## Medium Priority
- [ ] Document internal Rule Engine logic inside `ARCHITECTURE.md`.
- [ ] Write integration test validating `epic doctor` behavior against missing Rust dependencies.
